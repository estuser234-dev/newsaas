'use client';
import { useState } from 'react';import { calcCompound, Frequency } from '@/lib/calculators';import { formatCurrency } from '@/lib/formatters';import { ResponsiveContainer,LineChart,Line,XAxis,YAxis,Tooltip } from 'recharts';
export default function Page(){const[c,setC]=useState('USD');const[d,setD]=useState({p:'10000',m:'500',r:'8',y:'10',f:'monthly'});const[r,setR]=useState<ReturnType<typeof calcCompound>|null>(null);const run=()=>setR(calcCompound(+d.p,+d.m,+d.r,+d.y,d.f as Frequency));
return <main className='container-main py-10'><h1 className='text-3xl font-bold'>Compound Interest Calculator</h1><div className='card mt-6 grid gap-3 p-6 md:grid-cols-3'>{Object.entries(d).map(([k,v])=>k!=='f'?<input key={k} className='rounded border p-2' value={v} onChange={e=>setD({...d,[k]:e.target.value})}/>:<select key={k} value={d.f} className='rounded border p-2' onChange={e=>setD({...d,f:e.target.value})}><option value='monthly'>monthly</option><option value='quarterly'>quarterly</option><option value='yearly'>yearly</option></select>)}<select className='rounded border p-2' value={c} onChange={e=>setC(e.target.value)}><option>USD</option><option>PKR</option><option>QAR</option></select></div><button onClick={run} className='mt-4 rounded bg-blue-600 px-4 py-2 text-white'>Calculate</button>{r&&<div className='mt-6 space-y-4'><div className='card p-4'><p>Total invested: {formatCurrency(r.totalInvested,c)}</p><p>Profit: {formatCurrency(r.profit,c)}</p><p>Final value: {formatCurrency(r.finalValue,c)}</p></div><div className='card h-72 p-4'><ResponsiveContainer width='100%' height='100%'><LineChart data={r.rows}><XAxis dataKey='year'/><YAxis/><Tooltip/><Line type='monotone' dataKey='value' stroke='#2563eb'/></LineChart></ResponsiveContainer></div></div>}</main>}
